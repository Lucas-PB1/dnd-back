---
description: TypeORM no dnd-api — schema rpg, views PHB, pooler (além da skill global typeorm)
globs: src/**/*.ts
alwaysApply: false
---

# TypeORM (dnd-api)

1. Skill global `typeorm` (+ `nestjs`).
2. Overlay local: `typeorm-rpg-entities` (schema `rpg`, `phb_*`, `v_phb_*`).
3. `synchronize: false`; DDL em `database/`.
4. `DATABASE_URL` pooler Supabase 6543 em serverless.
5. Não expor entity na API — mapper/DTO.
