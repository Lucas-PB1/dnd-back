/** Juramentos de paladino — notas estáticas em `phb_level_combat_note`. */

export function addPaladinSubclassNotes(
  _notes: string[],
  _subclassSlug: string | null | undefined,
  _level: number,
): void {
  // migrado para catálogo
}
